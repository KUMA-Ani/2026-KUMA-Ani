import { defineConfig } from 'astro/config';

export default defineConfig({
  // 사이트의 기본 경로를 저장소 이름과 똑같이 맞춰줍니다.
  base: '/2026-KUMA-Ani', 
  // 실제 배포될 도메인 주소 (선택사항이지만 권장)
  site: 'https://2026-kuma-ani.vercel.app',
});
